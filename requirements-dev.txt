# PolarPandas — development dependencies
# Install with: pip install -r requirements-dev.txt

# ── Runtime (same as pyproject.toml dependencies) ─────────────────────────
pandas>=2.0
polars>=0.20
matplotlib>=3.7

# ── Testing ───────────────────────────────────────────────────────────────
pytest>=8.0
pytest-cov>=5.0        # coverage reports:  pytest --cov=src

# ── Code quality (optional but recommended) ───────────────────────────────
ruff>=0.4              # linter + formatter: ruff check src/
mypy>=1.10             # type checker:       mypy src/